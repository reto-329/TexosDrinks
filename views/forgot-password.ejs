<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Texos Drinks:: Forgot Password</title>
    <link rel="stylesheet" href="/css/header-responsive.css">
    <link rel="stylesheet" href="/css/common.css">
    <link rel="stylesheet" href="/css/header.css">
    <link rel="stylesheet" href="/css/auth.css">
    <link rel="stylesheet" href="/css/mobile-buttons.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="icon" type="image/png" href="/Images/logo1.png" sizes="256x256">
    <style>
        .auth-container {
            max-width: 450px;
            margin: 2rem auto;
            padding: 2rem;
            background: #fff;
            border-radius: 10px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
        }
        
        .desktop-submit-btn {
            display: block;
            width: 100%;
            margin-bottom: 10px;
            background-color: #4CAF50;
        }
        
        .mobile-ok-btn {
            display: none;
            width: 100%;
            margin-bottom: 10px;
            background-color: #4CAF50;
        }
        
        /* Mobile-specific styles */
        @media (max-width: 768px) {
            .mobile-ok-btn {
                display: block;
            }
            
            .desktop-submit-btn {
                display: none;
            }
        }
        
        .auth-header {
            text-align: center;
            margin-bottom: 2rem;
        }
        
        .auth-header img {
            max-width: 120px;
            margin-bottom: 1rem;
        }
        
        .auth-header h1 {
            font-size: 1.75rem;
            color: #333;
            margin-bottom: 0.5rem;
        }
        
        .auth-header p {
            color: #666;
            font-size: 0.95rem;
        }
        
        .form-group {
            margin-bottom: 1.5rem;
        }
        
        .form-group label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: 500;
            color: #333;
        }
        
        .form-control {
            width: 100%;
            padding: 0.75rem 1rem;
            border: 1px solid #ddd;
            border-radius: 4px;
            font-size: 1rem;
            transition: border-color 0.2s;
        }
        
        .form-control:focus {
            border-color: var(--primary-color);
            outline: none;
        }
        
        .btn-primary {
            width: 100%;
            background: var(--primary-color);
            color: white;
            border: none;
            padding: 0.75rem 1rem;
            border-radius: 4px;
            font-weight: 600;
            cursor: pointer;
            transition: background 0.2s;
            font-size: 1rem;
        }
        
        .btn-primary:hover {
            background: var(--primary-hover);
        }
        
        .auth-footer {
            text-align: center;
            margin-top: 1.5rem;
            font-size: 0.9rem;
            color: #666;
        }
        
        .auth-footer a {
            color: var(--primary-color);
            text-decoration: none;
            font-weight: 500;
        }
        
        .auth-footer a:hover {
            text-decoration: underline;
        }
        
        .alert {
            padding: 1rem;
            border-radius: 4px;
            margin-bottom: 1.5rem;
            font-size: 0.9rem;
        }
        
        .alert-success {
            background-color: rgba(39, 174, 96, 0.1);
            color: #27ae60;
            border: 1px solid rgba(39, 174, 96, 0.2);
        }
        
        .alert-danger {
            background-color: rgba(231, 76, 60, 0.1);
            color: #e74c3c;
            border: 1px solid rgba(231, 76, 60, 0.2);
        }
        
        .otp-container {
            display: none;
            width: 100%;
            max-width: 500px;
            margin: 0 auto;
            padding: 1rem;
            box-sizing: border-box;
        }

        .otp-inputs {
            display: flex;
            justify-content: space-between;
            gap: 0.5rem;
            margin-bottom: 1.5rem;
            width: 100%;
        }

        .otp-input {
            flex: 1;
            min-width: 40px;
            height: 50px;
            text-align: center;
            font-size: 1.5rem;
            border: 1px solid #ddd;
            border-radius: 4px;
            box-sizing: border-box;
        }

        .otp-input:focus {
            border-color: var(--primary-color);
            outline: none;
            box-shadow: 0 0 0 2px rgba(var(--primary-color-rgb), 0.2);
        }

        /* Responsive adjustments */
        @media (max-width: 768px) {
            .otp-inputs {
                margin-bottom: 1rem;
            }
            
            .otp-input {
                height: 45px;
                font-size: 1.3rem;
            }
        }

        @media (max-width: 480px) {
            .otp-inputs {
                gap: 0.25rem;
            }
            
            .otp-input {
                height: 40px;
                font-size: 1.1rem;
                min-width: 35px;
            }
        }

        @media (max-width: 360px) {
            .otp-input {
                height: 35px;
                font-size: 1rem;
                min-width: 30px;
            }
        }
    </style>
</head>
<body>
    <%- include('partials/headerSelector', { user: user || null }) %>
    
    <div class="auth-container">
        <div class="auth-header">
            <img src="/Images/logo1.png" alt="TexosDrinks Logo">
            <h1>Forgot Password</h1>
            <p>Enter your email to reset your password</p>
        </div>
        
        <div id="alert-container">
            <% if (typeof message !== 'undefined' && message) { %>
                <div class="alert alert-danger">
                    <i class="fas fa-exclamation-circle"></i> <%= message %>
                </div>
            <% } %>
        </div>
        
        <div id="email-form-container">
            <form id="email-form">
                <div class="form-group">
                    <label for="email">Email Address</label>
                    <input type="email" id="email" name="email" class="form-control" required placeholder="Enter your email">
                </div>
                
                <button type="button" id="manual-email-btn" class="btn-primary mobile-ok-btn">Send Reset Code</button>
                <button type="submit" class="btn-primary desktop-submit-btn">Send Reset Code</button>
            </form>
        </div>
        
        <div id="otp-form-container" class="otp-container">
            <p class="text-center mb-4">We've sent a 6-digit code to your email. Enter the code below to continue.</p>
            
            <form id="otp-form">
                <input type="hidden" id="reset-email" name="email">
                
                <div class="form-group">
                    <label for="otp">Verification Code</label>
                    <div class="otp-inputs">
                        <input type="text" class="otp-input" maxlength="1" pattern="[0-9]" required>
                        <input type="text" class="otp-input" maxlength="1" pattern="[0-9]" required>
                        <input type="text" class="otp-input" maxlength="1" pattern="[0-9]" required>
                        <input type="text" class="otp-input" maxlength="1" pattern="[0-9]" required>
                        <input type="text" class="otp-input" maxlength="1" pattern="[0-9]" required>
                        <input type="text" class="otp-input" maxlength="1" pattern="[0-9]" required>
                    </div>
                </div>
                
                <button type="button" id="manual-otp-btn" class="btn-primary mobile-ok-btn">Verify Code</button>
                <button type="submit" class="btn-primary desktop-submit-btn">Verify Code</button>
            </form>
        </div>
        
        <div class="auth-footer">
            <p>Remember your password? <a href="/login">Log In</a></p>
        </div>
    </div>
    
    <%- include('partials/footer') %>
    
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const emailForm = document.getElementById('email-form');
            const otpForm = document.getElementById('otp-form');
            const emailFormContainer = document.getElementById('email-form-container');
            const otpFormContainer = document.getElementById('otp-form-container');
            const alertContainer = document.getElementById('alert-container');
            const resetEmailInput = document.getElementById('reset-email');
            const otpInputs = document.querySelectorAll('.otp-input');
            
            // Handle OTP input behavior
            otpInputs.forEach((input, index) => {
                // Auto-focus next input
                input.addEventListener('input', function() {
                    if (this.value.length === 1) {
                        if (index < otpInputs.length - 1) {
                            otpInputs[index + 1].focus();
                        }
                    }
                });
                
                // Handle backspace
                input.addEventListener('keydown', function(e) {
                    if (e.key === 'Backspace' && !this.value) {
                        if (index > 0) {
                            otpInputs[index - 1].focus();
                        }
                    }
                });
            });
            
            // Manual OK button for email form
            document.getElementById('manual-email-btn').addEventListener('click', function() {
                emailForm.dispatchEvent(new Event('submit'));
            });
            
            // Manual OK button for OTP form
            document.getElementById('manual-otp-btn').addEventListener('click', function() {
                let allFilled = true;
                otpInputs.forEach(input => {
                    if (!input.value) {
                        allFilled = false;
                    }
                });
                
                if (allFilled) {
                    otpForm.dispatchEvent(new Event('submit'));
                } else {
                    alertContainer.innerHTML = `
                        <div class="alert alert-danger">
                            <i class="fas fa-exclamation-circle"></i> Please enter all 6 digits of the verification code.
                        </div>
                    `;
                }
            });
            
            // Email form submission
            emailForm.addEventListener('submit', async function(e) {
                e.preventDefault();
                
                const email = document.getElementById('email').value;
                
                try {
                    const response = await fetch('/api/users/forgot-password', {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json'
                        },
                        body: JSON.stringify({ email })
                    });
                    
                    const data = await response.json();
                    
                    if (response.ok) {
                        alertContainer.innerHTML = `
                            <div class="alert alert-success">
                                <i class="fas fa-check-circle"></i> ${data.message}
                            </div>
                        `;
                        
                        emailFormContainer.style.display = 'none';
                        otpFormContainer.style.display = 'block';
                        resetEmailInput.value = email;
                        otpInputs[0].focus();
                    } else {
                        alertContainer.innerHTML = `
                            <div class="alert alert-danger">
                                <i class="fas fa-exclamation-circle"></i> ${data.message || 'Failed to send reset code'}
                            </div>
                        `;
                    }
                } catch (error) {
                    console.error('Error sending reset code:', error);
                    alertContainer.innerHTML = `
                        <div class="alert alert-danger">
                            <i class="fas fa-exclamation-circle"></i> An error occurred. Please try again.
                        </div>
                    `;
                }
            });
            
            // OTP form submission
            otpForm.addEventListener('submit', async function(e) {
                e.preventDefault();
                
                const email = resetEmailInput.value;
                let otp = '';
                
                otpInputs.forEach(input => {
                    otp += input.value;
                });
                
                try {
                    const response = await fetch('/api/users/verify-reset-otp', {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json'
                        },
                        body: JSON.stringify({ email, otp })
                    });
                    
                    const data = await response.json();
                    
                    if (response.ok) {
                        window.location.href = `/reset-password?email=${encodeURIComponent(email)}&token=${encodeURIComponent(data.token)}`;
                    } else {
                        alertContainer.innerHTML = `
                            <div class="alert alert-danger">
                                <i class="fas fa-exclamation-circle"></i> ${data.message || 'Invalid verification code'}
                            </div>
                        `;
                    }
                } catch (error) {
                    console.error('Error verifying code:', error);
                    alertContainer.innerHTML = `
                        <div class="alert alert-danger">
                            <i class="fas fa-exclamation-circle"></i> An error occurred. Please try again.
                        </div>
                    `;
                }
            });
        });
    </script>
</body>
</html>